import { MovieDetailsDirective } from './movie-details.directive';

describe('MovieDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
